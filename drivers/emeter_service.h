#ifndef EMETER_SERVICE_H
#define EMETER_SERVICE_H

/* Initialize the MAX78615, create its 10 Hz sampler, and register all
   protocol reporters. */
void emeter_service_init(void);

#endif /* EMETER_SERVICE_H */
